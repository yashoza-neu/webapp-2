version: 2

jobs:
  build:
    docker:
      - image: circleci/node:7.10
    
    steps:
      - checkout

      - run:
          name: install nodejs
          command: |
            apt-get update
            apt install curl -y
            apt-get install nodejs -y
            apt-get install wget zip unzip python2.7 python-pip -y
            node --version

      - run:
          name: change Directory
          command: |
            cd webapp
            ls -ax

      - run:
          name: update-npm
          command: npm install

      - run:
          name: start server
          command: npm start

      - run:
          name: test
          command: npm test